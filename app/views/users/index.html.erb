<% @users.each do |user| %>
  <p>
    <%= image_tag user.image %><br>
    <strong>Name:</strong> <%= user.first_name %> <%= user.last_name %><br>
    <strong>Location:</strong> <%= user.current_city %>
  </p>
<% end %>
